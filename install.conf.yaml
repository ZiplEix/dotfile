- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.gitconfig:
    ~/.gitconfig:
    ~/.config:
      create: true
      path: config

- create:
    - ~/delivery

- shell:
    -
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
